---
description: Open the current idleai ad in your browser (a click pays 50× a view)
allowed-tools: Bash(node:*)
---

Run this exact command and show the user its output:

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/open-ad.mjs"
```
