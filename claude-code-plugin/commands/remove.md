---
description: Remove the idleai statusline from Claude Code settings
allowed-tools: Bash(node:*)
---

Run this exact command and show the user its output:

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/setup.mjs" --remove
```
